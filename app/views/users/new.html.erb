<h1>新規ユーザー作成</h1>
<%= form_with model: @user, local: true do |f| %>
  <%= f.label :uid %>
  <%= f.text_field :uid %>

  <%= f.label :pass %>
  <%= f.password_field :pass %>

  <%= f.submit '作成' %>
<% end %>